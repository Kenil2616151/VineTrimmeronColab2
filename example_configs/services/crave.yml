# This is an example user config file. Shown values are guided *examples* only!

# this is a very minimal config used only for auth until Crave has a proper automated auth system in place.
auth: 'eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9.eyJsb2Nhb...'  # should be a JWT bearer as provided by a Crave request
